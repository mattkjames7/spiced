diff --git a/src/networkfunc.cc b/src/networkfunc.cc
index 8816ef8..29361ae 100644
--- a/src/networkfunc.cc
+++ b/src/networkfunc.cc
@@ -1,4 +1,5 @@
 #include "networkfunc.h"
+#include <vector>
 
 namespace ann {
 
@@ -38,8 +39,8 @@ NetworkFunc::NetworkFunc(int L, int *s, const char * sHiddenFunc,
 	
 	/* define the shapes of the matrices */
 	int nm = L_ - 1;
-	int ws[nm*2];
-	int bs[nm*2];
+	std::vector<int> ws(nm*2);
+	std::vector<int> bs(nm*2);
 	
 	for (i=0;i<nm;i++) {
 		ws[i*2] = s_[i];
@@ -50,8 +51,8 @@ NetworkFunc::NetworkFunc(int L, int *s, const char * sHiddenFunc,
 	}
 	
 	/* create some empty matrices */
-	W_ = new MatrixArray(nm,ws);
-	B_ = new MatrixArray(nm,bs);
+	W_ = new MatrixArray(nm,ws.data());
+	B_ = new MatrixArray(nm,bs.data());
 	
 	/* empty scaling factors */
 	scale0_ = new float[s_[L_-1]];
@@ -275,14 +276,14 @@ MatrixArray* NetworkFunc::_CreatePropagationMatrices(int n) {
 	/* get the matrix shapes first */
 	int i;
 	int nMat = L_;
-	int mShapes[nMat*2];
+	std::vector<int> mShapes(nMat*2);
 	for (i=0;i<L_;i++) {
 		mShapes[i*2 + 1] = s_[i];
 		mShapes[i*2] = n;
 	}
 	
 	/* create the array */
-	MatrixArray *out = new MatrixArray(nMat,mShapes);
+	MatrixArray *out = new MatrixArray(nMat,mShapes.data());
 
 	return out;
 	
diff --git a/src/regularization.cc b/src/regularization.cc
index bbb7404..63481bc 100644
--- a/src/regularization.cc
+++ b/src/regularization.cc
@@ -26,7 +26,7 @@ float L1Regularization(MatrixArray &w, float L1, int m) {
        nk = w.matrix[i]->shape[1];
        for (j=0;j<nj;j++) {
            for (k=0;k<nk;k++) {
-                J += fabsf(w.matrix[i]->data[j][k]);
+                J += fabs(w.matrix[i]->data[j][k]);
            }
        }
    }
